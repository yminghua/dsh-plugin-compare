# dsh-plugin-compare host and web-client bundle.
- insert:
  - id: dsh-plugin-compare
    name: dsh-plugin-compare
    config:
      maxRuns: 100
      runTimeoutMs: 900000
      checkTimeoutMs: 300000
      maxTrials: 10
