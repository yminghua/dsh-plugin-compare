# Public screenshots

This directory is reserved for reviewed, real screenshots. No screenshots have been published yet.

Capture originals in the ignored `example/captures/<run>/` directory printed by `prepare.mjs`.
After reviewing and masking sensitive information, place selected publication copies here:

1. `01-initial-tests.png` — 5 initial tests: 1 pass, 4 fail.
2. `02-controlled-config.png` — same model, two presets, one pair, `node --test`.
3. `03-running.png` — actual pair / variant / phase / elapsed time.
4. `04-result-overview.png` — plugin identity, check outcomes and measured deltas.
5. `05-evidence.png` — optional: test output and Git snapshots.

Avoid unrelated tabs, browser profiles, credentials, private paths, and personal details.
Do not edit measurements or fabricate a successful state. Add images to the walkthrough only after the files exist.
